[Unit]
Description=OTMS Frontend React App
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/frontend
ExecStart=/usr/bin/npx serve -s build -l 3000
Restart=on-failure

[Install]
WantedBy=multi-user.target
